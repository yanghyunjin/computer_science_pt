# 🐍 Python Practice Quiz - Set 2  
_No starter code — keep practicing your basics!_  
`input · print · if · for · range · len`

---

1. **Temperature Converter**  
   Ask for the temperature in Celsius and print it in Fahrenheit.  
   Formula: `F = (C × 9/5) + 32`  
   > 💡 **Hint:** Use `float()` for decimals and string formatting in `print()`.

---

2. **Positive or Negative**  
   Ask the user for a number.  
   Print “Positive”, “Negative”, or “Zero”.  
   > 💡 **Hint:** Use `if`, `elif`, `else`.

---

3. **Countdown by Twos**  
   Print numbers from **10 down to 0**, counting by 2.  
   > 💡 **Hint:** `range(10, -1, -2)`

---

4. **Sum of Even Numbers (1–10)**  
   Calculate and print the sum of all even numbers between 1 and 10.  
   > 💡 **Hint:** Use `if num % 2 == 0`.

---

5. **Password Checker**  
   Ask the user to enter a password.  
   If the password equals `"python123"`, print “Access granted.”  
   Otherwise, print “Access denied.”  
   > 💡 **Hint:** String comparison uses `==`.

---

6. **Favorite Animal Loop**  
   Ask for your favorite animal and print it **4 times**.  
   > 💡 **Hint:** Use `range(4)`.

---

7. **Average of 3 Numbers**  
   Ask the user for 3 numbers and print their average.  
   > 💡 **Hint:** `(a + b + c) / 3`

---

8. **Simple Counter**  
   Ask how many times to print “Python is fun!” and repeat that many times.  
   > 💡 **Hint:** Use the number from `input()` in `range()`.

---

9. **Find the Largest**  
   Ask for two numbers and print which one is larger (or if they are equal).  
   > 💡 **Hint:** Use `if`, `elif`, `else`.

---

10. **First Letter Extractor**  
    Ask for a word and print its first letter.  
    > 💡 **Hint:** Use indexing like `word[0]`.

---

# 🐍 Python Practice Quiz - Set 3  
_Level up your basics!_  
`input · print · if · for · range · str`

---

1. **Double the Number**  
   Ask for a number and print double its value.  
   > 💡 **Hint:** Use `int()` or `float()` and multiply by 2.

---

2. **Name Length Checker**  
   Ask for your full name and print:  
   “Your name has ___ letters.”  
   > 💡 **Hint:** Use `len()` and remove spaces using `.replace(" ", "")`.

---

3. **Count to N**  
   Ask for a number **N**, then print numbers from 1 to N.  
   > 💡 **Hint:** Use `range(1, N+1)`.

---

4. **Even Number Printer**  
   Print even numbers between 1 and 20.  
   > 💡 **Hint:** Use `range(2, 21, 2)`.

---

5. **Simple Quiz**  
   Ask: “What is 5 + 3?”  
   If the user answers `8`, print “Correct!”, else print “Try again!”.  
   > 💡 **Hint:** Convert input to `int()`.

---

6. **Character Loop**  
   Ask for a word and print each letter on a new line.  
   > 💡 **Hint:** Use a `for` loop to iterate over the string.

---

7. **Reverse Countdown**  
   Ask for a number and print from that number down to 1.  
   > 💡 **Hint:** Use `range(num, 0, -1)`.

---

8. **Square Numbers**  
   Print the squares of numbers 1 to 5.  
   > 💡 **Hint:** Use `num ** 2`.

---

9. **Favorite Food Sentence**  
   Ask for your favorite food and print:  
   “Mmm… I could eat ___ all day!”  
   > 💡 **Hint:** Combine text with variables.

---

10. **Word Reverser**  
    Ask for a word and print it backwards.  
    > 💡 **Hint:** Use slicing `word[::-1]`.

---

💾 **Tip:**  
Keep each set in its own folder:  
`set1/`, `set2/`, `set3/` — each with `q1.py` to `q10.py`.
