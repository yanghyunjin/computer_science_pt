# 🐍 Python Practice Quiz - Set 4  
_Let’s keep going! Practice makes perfect._  
`input · print · if · for · range · str · int`

---

1. **Greeting with Age**  
   Ask for the user’s name and age, then print:  
   “Hello ___, you are ___ years old!”  
   > 💡 **Hint:** Use commas in `print()` to combine text and numbers.

---

2. **Number Comparison**  
   Ask for two numbers and print whether the first is greater, smaller, or equal to the second.  
   > 💡 **Hint:** Use `if`, `elif`, `else`.

---

3. **Multiply by 10**  
   Ask for a number and print it multiplied by 10.  
   > 💡 **Hint:** Convert input using `int()`.

---

4. **Odd Numbers Only**  
   Print only odd numbers from 1 to 10.  
   > 💡 **Hint:** Use `range(1, 11, 2)`.

---

5. **Name Repeater by Length**  
   Ask for your name and print it as many times as there are letters.  
   > 💡 **Hint:** Use `len()` inside `range()`.

---

6. **Total of 1 to N**  
   Ask for a number N and print the sum of numbers from 1 to N.  
   > 💡 **Hint:** Use `for` and accumulate in a variable.

---

7. **Uppercase Converter**  
   Ask for a word and print it in all uppercase letters.  
   > 💡 **Hint:** Use `.upper()`.

---

8. **Number Skip**  
   Print numbers from 1 to 10 but skip number 5.  
   > 💡 **Hint:** Use `continue` in a loop.

---

9. **Square Until 5**  
   Print each number from 1 to 5 and its square (e.g., “2 squared is 4”).  
   > 💡 **Hint:** Use formatted strings like `f"{n} squared is {n**2}"`.

---

10. **Last Letter Extractor**  
    Ask for a word and print its last letter.  
    > 💡 **Hint:** Use negative indexing `word[-1]`.

---

# 🐍 Python Practice Quiz - Set 5  
_Final beginner challenge — you’re almost ready for intermediate Python!_  
`input · print · if · for · str · list · len`

---

1. **Welcome Message**  
   Ask for the user’s name and favorite subject, then print:  
   “Welcome ___! I hope you enjoy learning ___.”  
   > 💡 **Hint:** Combine two inputs in one message.

---

2. **Check Divisible by 3**  
   Ask for a number and print whether it’s divisible by 3.  
   > 💡 **Hint:** Use `if num % 3 == 0`.

---

3. **Letter Counter**  
   Ask for a sentence and print how many characters it contains (excluding spaces).  
   > 💡 **Hint:** Use `.replace(" ", "")` before `len()`.

---

4. **Multiplication Table (1–10)**  
   Ask for a number and print its times table up to 10.  
   > 💡 **Hint:** Loop with `range(1, 11)`.

---

5. **Countdown Blastoff**  
   Print numbers from 3 down to 1, then print “Blastoff!”.  
   > 💡 **Hint:** `range(3, 0, -1)`.

---

6. **Vowel Counter**  
   Ask for a word and count how many vowels (a, e, i, o, u) it contains.  
   > 💡 **Hint:** Use `for ch in word:` and check if `ch in "aeiou"`.

---

7. **Word Echo**  
   Ask for a word and number N, then print that word N times on one line.  
   > 💡 **Hint:** Use string repetition: `word * n`.

---

8. **Smallest of Three**  
   Ask for three numbers and print the smallest.  
   > 💡 **Hint:** Use `min()`.

---

9. **Reverse Name Letters**  
   Ask for your name and print each letter in reverse order (one per line).  
   > 💡 **Hint:** Loop over `reversed(name)`.

---

10. **Simple Calculator**  
    Ask for two numbers and an operation (+, -, *, /), then print the result.  
    > 💡 **Hint:** Use `if` / `elif` to handle each operation.

---

💾 **Tip:**  
By now, you can try mixing questions — e.g., combine input, conditionals, and loops in one mini-project!
